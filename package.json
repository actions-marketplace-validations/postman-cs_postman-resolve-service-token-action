{
  "name": "@postman-cse/onboarding-resolve-service-token",
  "version": "0.1.0",
  "description": "Public open-alpha Postman service token resolver GitHub Action and CLI.",
  "type": "module",
  "main": "dist/index.cjs",
  "bin": {
    "postman-resolve-service-token": "dist/cli.cjs"
  },
  "files": [
    "action.yml",
    "dist"
  ],
  "scripts": {
    "build": "npm run typecheck && rm -rf dist && esbuild src/main.ts --bundle --platform=node --target=node24 --format=cjs --outfile=dist/index.cjs && esbuild src/cli.ts --bundle --platform=node --target=node24 --format=cjs --banner:js='#!/usr/bin/env node' --outfile=dist/cli.cjs",
    "check:dist": "npm run build && git diff --exit-code -- dist",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "keywords": [
    "github-action",
    "postman"
  ],
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "overrides": {
    "undici": "^6.24.0",
    "picomatch": ">=4.0.4"
  },
  "dependencies": {
    "@actions/core": "^1.11.1"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "@eslint/js": "^10.0.1",
    "@types/node": "^25.3.5",
    "esbuild": "^0.27.3",
    "eslint": "^10.1.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.57.2",
    "vitest": "^4.0.18"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/postman-cs/postman-resolve-service-token-action"
  },
  "engines": {
    "node": ">=24"
  }
}
